rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read access to all files, so anyone can see images like the logo.
    // Allow write access only for authenticated (logged-in) users.
    match /{allPaths=**} {
      allow read;
      allow write: if request.auth != null;
    }
  }
}
